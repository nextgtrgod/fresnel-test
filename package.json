{
  "scripts": {
    "build": "parcel build index.html --no-source-maps --public-url ./"
  },
  "dependencies": {
    "three": "^0.125.0"
  },
  "devDependencies": {
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1"
  }
}
